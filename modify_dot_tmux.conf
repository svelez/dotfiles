#!/usr/bin/env bash

function has() {
	echo "$orig" | grep "$1" > /dev/null
}

function addLine() {
	if ! has "$1"; then
		echo "$1"
	fi
}

orig=$(cat)
echo "$orig"

addLine 'set -g default-terminal "screen-256color"'
addLine 'set -g mouse on'
